<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="CoinMapper">

	<update id="bitInsert" parameterType="string">
 	update bit
	set bit = 1
	where num = 1
    </update>

	<select id="bitCount" resultType="int">
     select count(bit)
     from bit
    </select>
     
	<insert id="bitNumInsert" parameterType="int">
       insert into bit(num)
       values(#{num})
    </insert>
    
	<select id="bitNumCount" resultType="int">
     select count(num)
     from bit
    </select>
    
    <update id="bitoneupdate">
    update bit
	set bit =null
	where num = 1
    </update>
   
    <update id="bitallupdate" parameterType="int">
	update bit
	set bit =(select bit from bit where num=#{num}+1)
	where num = #{num}
    </update>
    
    
    <select id ="selectnotnull" parameterType="int">
    select bit from bit where num=#{num};
    </select>
    

</mapper>